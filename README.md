# ObsidianOS Migration Scripts
A repo for ObsidianOS Migrations.

## Usage

```sh
sudo obsidianctl migrate run 0  # run migration script 0
```

```sh
sudo obsidianctl migrate rollback 0  # run migration rollback 0 (if exists)
```

```sh
sudo obsidianctl migrate list  # list applied migrations
```
